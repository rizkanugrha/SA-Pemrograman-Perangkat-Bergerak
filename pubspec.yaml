name: p03_provider_crud
description: Remedial Task Tracker — P03 Form/CRUD/Provider. Shell ChangeNotifier + form; inti CRUD sengaja ditinggalkan TODO.
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.4.0
  flutter: ">=3.22.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  provider: ^6.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
